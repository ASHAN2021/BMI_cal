import 'package:flutter/material.dart';

const kTextstyle =
    TextStyle(color: Colors.orange, fontSize: 50, fontWeight: FontWeight.bold);

const kbuttonstyleadd = Icon(
  Icons.add,
  size: 40,
);

const kbuttonstyleremove = Icon(
  Icons.remove,
  size: 40,
);
